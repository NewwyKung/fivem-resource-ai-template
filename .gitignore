# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency and build caches
node_modules/
jspm_packages/
web_modules/
.npm
.eslintcache
.stylelintcache
.parcel-cache
.svelte-kit/
**/.vitepress/dist
**/.vitepress/cache
.docusaurus
.serverless/
.fusebox/
.firebase/
.tern-port
.vscode-test
*.tsbuildinfo

# General generated output
dist/
out/
build/Release/
coverage/
.nyc_output/

# Environment and package artifacts
.env
.env.*
!.env.example

# Local per-developer MCP dev-bridge config/secrets (see examples/capabilities/mcp-dev-bridge/).
# Keep only the .example versions tracked. .template-sync.json is intentionally NOT
# ignored — it's meant to be committed so the team can see what template state was
# last synced.
.mcp-config.json
.cursor/mcp.json
*.tgz
.yarn-integrity
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite temporary files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Generated NUI output. Build locally or through the release pipeline.
resource/html/*
!resource/html/.gitkeep

# Generated release packages
release/*
!release/.gitkeep

# OS/editor files
.DS_Store
.idea/
